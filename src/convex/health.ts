import { query } from "./_generated/server";

/**
 * ═══════════════════════════════════════════════════════════════
 * 🛡 نبضة الصحة (Health Ping) — أرخص دالة ممكنة في النظام
 * ═══════════════════════════════════════════════════════════════
 *
 * لا تقرأ أي صف من قاعدة البيانات ولا تكتب شيئاً — استدعاء واحد،
 * وقراءة صفرية (0 database reads). تُستخدم فقط لاستفسار «هل تعافى
 * الخادم؟» بعد انقطاع أو تجاوز حصة، فلا تُكلّف شيئاً في الحصة.
 *
 * لا تُستدعى من أي واجهة، ولا تُشترك اشتراكاً تفاعلياً أبداً.
 */
export const ping = query({
  args: {},
  handler: async () => ({ ok: true as const, at: Date.now() }),
});
