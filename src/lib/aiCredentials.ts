/**
 * ═══════════════════════════════════════════════════════════════
 * بيانات الاعتماد — لا تُخزَّن أي مفاتيح هنا إطلاقاً.
 *
 * حُذفت المفاتيح المشفّرة وقراءة env نهائياً. النظام الوحيد:
 *   النظام الأول (System A): مفتاح API + رابط المزوّد — من مركز API.
 *   النظام الثاني (System B): مفتاح API فقط — من مركز API.
 *
 * المحرك الوحيد (aiConfig.callLlm) يقرأ النظامين المضبوطين من قاعدة
 * البيانات عبر ensureAiRuntime — لا يوجد أي مصدر مفتاح آخر في اللعبة.
 * ═══════════════════════════════════════════════════════════════
 */

/** لا يوجد مفتاح أساسي ثابت — تُدار المفاتيح من مركز API حصراً. */
export const ADMIN_AI_KEY = "";

/** لا مفاتيح احتياطية. */
export const BACKUP_AI_KEY = "";

/** علامة توافق قديمة (لا تُستخدم في أي استدعاء). */
export const ADMIN_AI_PROVIDER = "api-center";

export const ADMIN_AI_MODEL = "openrouter/auto";

export const AI_CREDENTIALS_README =
  "أُزيلت المفاتيح نهائياً (المشفّرة وenv). تُدار المفاتيح من مركز API — نظامان فقط: (1) مفتاح + رابط، (2) مفتاح فقط — مع تحقق حقيقي وسجل إثبات.";
