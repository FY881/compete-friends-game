/**
 * ═══════════════════════════════════════════════════════════════
 * بيانات الاعتماد — لا تُخزَّن أي مفاتيح هنا بعد الآن.
 *
 * أُزيل المفتاح الرسمي المشفّر نهائياً. النظام الجديد:
 *   النظام الأول (System A): مفتاح + رابط — يُدار من مركز API.
 *   النظام الثاني (System B): مفتاح فقط — يُدار من مركز API.
 *
 * تُقرأ المفاتيح من:
 *   1. ضبط runtime (من apiCore عبر الواجهة)
 *   2. متغيرات البيئة (env) كمصدر احتياطي تديره اللعبة
 * ═══════════════════════════════════════════════════════════════
 */

// لا مفتاح مشفّر — يُقرأ من env فقط (يحل محله ضبط النظامين من مركز API).
const fromEnv = typeof process !== "undefined" ? process.env.OPENROUTER_API_KEY || "" : "";

/** المفتاح الأساسي (قد يكون فارغاً إن لم يُضبط — أنظمة API هي المرجع الآن) */
export const ADMIN_AI_KEY = fromEnv;

/** لا مفاتيح احتياطية مشفّرة */
export const BACKUP_AI_KEY = "";

export const ADMIN_AI_PROVIDER = "OpenRouter";

export const ADMIN_AI_MODEL = "openrouter/auto";

export const AI_CREDENTIALS_README =
  "أُزيلت المفاتيح المشفّرة نهائياً. تُدار المفاتيح من مركز API (نظامان فقط: مفتاح+رابط / مفتاح فقط) أو من متغيرات البيئة.";