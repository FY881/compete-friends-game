/**
 * أعلام البناء (Build Flags) — تُقيَّم وقت البناء لتحديد أي نسخة تُنتَج:
 *
 * 1) نسخة اللاعبين (الافتراضية): غرفة المالك موجودة (للويب الرسمي).
 *    `VITE_NO_OWNER=1` يزيل كل وصول للمالك — المسارات والأزرار —
 *    من ملف APK الخاص باللاعبين («حرب العقول بدون غرفة المالك»).
 *
 * 2) تطبيق المالك المستقل: `VITE_OWNER_APP=1` يفتح التطبيق مباشرة على
 *    تسجيل الدخول ثم لوحة التحكم الكاملة (بدون واجهة اللاعبين).
 *
 * القيم ثوابت تُحلَّل وقت البناء، فيُسقط البناء (minify) الفروع الميتة
 * من النسخة النهائية تلقائياً.
 */
export const OWNER_ROOM_ENABLED = import.meta.env.VITE_NO_OWNER !== "1";
export const OWNER_APP_MODE = import.meta.env.VITE_OWNER_APP === "1";
